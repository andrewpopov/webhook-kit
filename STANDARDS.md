# Shared package standards

The engineering and packaging standards for every `andrewpopov/*` package
live in **one** place — the single source of truth:

> `agent_brain/knowledge/shared-package-standards.md`

This repo intentionally keeps **no local copy**. A copy would drift from the
canonical and quietly lie; read (and edit) the file above instead.
