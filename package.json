{
  "name": "@andrewpopov/webhook-kit",
  "version": "0.1.2",
  "description": "Framework-agnostic outbound webhook delivery: HMAC-signed `${timestamp}.${body}` with sha256= + X-Webhook-Timestamp headers, fire-time SSRF re-check, per-attempt timeout, redirect: manual, and a receiver-side verifier.",
  "author": "Andrew Popov",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:pack": "node scripts/verify-pack.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
